// #todo

'use strict';

// first <- 'angus'
let first = 'angus';

// last <- 'croll'
let last = 'croll';

let greeting = 'hello';
// greeting <- 'hello ' + first + ' ' + last
greeting = 'hello ' + first + ' ' + last;

// greeting <- greeting + '!'
greeting = greeting + '!';
console.log(greeting);
